let logging_directory = "./src/dev/log"
let general_log = `${logging_directory}/log.txt`
let request_log = `${logging_directory}/request_log.txt`
let error_log = `${logging_directory}/error_log.txt`
module.exports = {
    logging_directory,
    general_log,
    request_log,
    error_log
    
}